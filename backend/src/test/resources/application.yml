spring:

  datasource:
    driver-class-name: org.h2.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

google:
  oauth:
    redirect-uri: "test"
    scope: "test"
    auth-url: "test"
    token-url: "test"
    userinfo-url: "test"
    client-id: "test"
    client-secret: "test"

  calendar:
    calendar-list-url: "test"
    subscribe-url: "test"
    unsubscribe-url: "test"

jwt:
  secret: "my-very-long-and-secure-secret-key-which-is-at-least-32-chars"
