import { style } from '@vanilla-extract/css';

export const containerStyle = style({
  width: '100%',
  minWidth: '1284px',
  minHeight: '100vh',
  padding: '56px 28px',
  display: 'flex',
  overflow: 'hidden',
  justifyContent: 'center',
});